/**
 * Appends " 2", " 3", etc. to `candidate` if it already exists in `existing`.
 * Returns the original name if no collision.
 */
export declare function dedupName(existing: string[], candidate: string): string;
//# sourceMappingURL=nameDedup.d.ts.map